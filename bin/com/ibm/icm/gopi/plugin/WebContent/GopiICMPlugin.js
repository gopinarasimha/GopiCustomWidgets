var icmContextRoot = "/GopiCustomWidget";
var paths = {
	"icmsample/gopi": icmContextRoot + "/icmsample/gopi"
};
require({paths:paths});

require([
	"dojo/_base/declare",
	"dojo/_base/lang"
], function(declare, lang) {
	/**
	 * Use this function to add any global JavaScript methods your plug-in requires.
	 */

});
